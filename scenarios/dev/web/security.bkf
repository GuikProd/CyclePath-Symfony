scenario
    name "Login"
    set env env
    endpoint env

    visit url('/en/login')
        expect status_code() == 200

scenario
    name "Register"
    set env env
    endpoint env
    follow_redirects true

    visit url('/en/register')
        expect status_code() == 200
        assert main.wall_time < 10ms

    submit button('registerBtn')
        param register[username] 'Yolo'
        param register[email] 'yolo@gmail.com'
        param register[plainPassword] 'Ie1FDLYOLO'
