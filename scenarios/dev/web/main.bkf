scenario
    name "Homepage"
    set env env
    endpoint env

    visit url('/')
        expect status_code() == 301

scenario
    name "Homepage - Locale"
    set env env
    endpoint env
    visit url('/en/')
        expect status_code() == 200
