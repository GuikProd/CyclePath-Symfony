apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: cyclepath
    tier: frontend
spec:
  # type: LoadBalancer
  ports:
  - port: 80
  selector:
    app: cyclepath
    tier: frontend
